#!/usr/bin/env zsh

# Checked-in command: local fixture verification only.
# Keep production publishing commands outside the repository so credentials,
# machine-specific paths, and live side effects cannot be triggered by copying
# this file.
set -euo pipefail

project_dir="${MARX_GROWTH_PROJECT_DIR:-$(pwd)}"
node_bin="${MARX_GROWTH_NODE:-node}"

cd "$project_dir"
"$node_bin" dist/cli/main.js run \
  --fixture tests/fixtures/moltbook.json \
  --dry-run \
  --real-model=false \
  --limit 100 \
  --actions 5
